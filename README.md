# Spam-Detection-Using-Python
Spam Message Detector is a Machine Learning project built using Python that classifies text messages as Spam or Normal (Ham). It uses TF-IDF Vectorization and the Naive Bayes algorithm to analyze messages and provide quick predictions through a simple user interface.
